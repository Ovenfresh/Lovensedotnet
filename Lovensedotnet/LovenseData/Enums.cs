namespace Data
{
    public enum LovenseApp
    {
        Connect = -1,
        Callback = 0,
        Remote = 1
    }
    public enum LovenseCommand
    {
        Function,
        GetToys,
        Pattern,
        Preset
    }
    public enum LovenseAction
    {
        Vibrate,
        Rotate,
        Pump
    }
}
